# ServicioSocial
git init
# Inicializar el repositorio Git local
git init

# Agregar todos los archivos al stage
git add .

# Realizar el primer commit
git commit -m "Primer commit: página web estática"

# Conectar con el repositorio remoto de GitHub
# (Reemplazar TU-USUARIO con su nombre de usuario de GitHub)
git remote add origin https://github.com/TU-USUARIO/mi-pagina-web.git

# Subir los archivos al repositorio
git push -u origin main

